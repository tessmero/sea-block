diff --git a/node_modules/three/build/three.core.js b/node_modules/three/build/three.core.js
index d32975a..21c8557 100644
--- a/node_modules/three/build/three.core.js
+++ b/node_modules/three/build/three.core.js
@@ -4359,6 +4359,7 @@ class Vector3 {
 	 * @param {number} [z=0] - The z value of this vector.
 	 */
 	constructor( x = 0, y = 0, z = 0 ) {
+    this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
 
 		/**
 		 * This flag can be used for type testing.
@@ -5627,6 +5628,7 @@ class Matrix3 {
 	 * @param {number} [n33] - 3-3 matrix element.
 	 */
 	constructor( n11, n12, n13, n21, n22, n23, n31, n32, n33 ) {
+    this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
 
 		/**
 		 * This flag can be used for type testing.
@@ -7682,6 +7684,7 @@ class Vector4 {
 	 * @param {number} [w=1] - The w value of this vector.
 	 */
 	constructor( x = 0, y = 0, z = 0, w = 1 ) {
+    this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
 
 		/**
 		 * This flag can be used for type testing.
@@ -9453,6 +9456,8 @@ class Box3 {
 	 */
 	constructor( min = new Vector3( + Infinity, + Infinity, + Infinity ), max = new Vector3( - Infinity, - Infinity, - Infinity ) ) {
 
+    this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
+
 		/**
 		 * This flag can be used for type testing.
 		 *
@@ -11373,6 +11378,7 @@ class Matrix4 {
 	 * @param {number} [n44] - 4-4 matrix element.
 	 */
 	constructor( n11, n12, n13, n14, n21, n22, n23, n24, n31, n32, n33, n34, n41, n42, n43, n44 ) {
+    this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
 
 		/**
 		 * This flag can be used for type testing.
@@ -13207,8 +13213,8 @@ class Object3D extends EventDispatcher {
 	 * Constructs a new 3D object.
 	 */
 	constructor() {
-
 		super();
+    this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
 
 		/**
 		 * This flag can be used for type testing.
@@ -15387,6 +15393,7 @@ class Color {
 	 * @param {number} [b] - The blue component.
 	 */
 	constructor( r, g, b ) {
+		this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
 		
 		/**
 		 * This flag can be used for type testing.
@@ -54626,6 +54633,7 @@ class Spherical {
 	 * @param {number} [theta=0] - The equator/azimuthal angle in radians around the y (up) axis.
 	 */
 	constructor( radius = 1, phi = 0, theta = 0 ) {
+		this.constructor.constructedCount = 1 + (this.constructor.constructedCount || 0);
 		
 		/**
 		 * The radius, or the Euclidean distance (straight-line distance) from the point to the origin.
